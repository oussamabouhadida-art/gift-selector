// ============================================================
//  CONFIG — fill these in before deploying.
//  Safe to commit the URL + anon key (they are public).
//  NEVER commit a service_role key. Use Row Level Security.
// ============================================================

window.SUPABASE_URL  = "https://YOUR-PROJECT.supabase.co";
window.SUPABASE_ANON = "YOUR-ANON-KEY";

// Change this to a private PIN you share only with yourself.
// Used to unlock /admin.html
window.ADMIN_PIN = "change-me-1234";
